import mlipx
import zntrack

from models import MODELS

project = zntrack.Project()

{% if datapath %} # lattice energy directory
lattice_energy_dir = "{{ datapath[0]}}"
{% endif %}

x23_dict = {}
for model_name, model in MODELS_subset.items():
    with project.group(model_name):
        benchmark = mlipx.CohesiveEnergies(
            lattice_energy_dir=lattice_energy_dir,
            model =model,
            model_name=model_name,
        )
        x23_dict[model_name] = benchmark
    

project.build()