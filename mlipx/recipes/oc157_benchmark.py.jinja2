{% if standalone is not defined %}
{% set standalone = true %}
{% endif %}

{% if standalone %}
import mlipx
import zntrack
from models import MODELS

project = zntrack.Project()
{% endif %}


oc_dict = {}
oc_list = []
for model_name, model in MODELS.items():
    with project.group(model_name, "oc157"):
        oc = mlipx.OC157Benchmark(
                model=model,
                model_name=model_name,
        )
        oc_dict[model_name] = oc
        oc_list.append(oc)
    

{% if standalone %}
project.build()
{% endif %}